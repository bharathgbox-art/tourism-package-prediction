# Dependencies used by all three GitHub Actions jobs
# Pin scikit-learn to the same version used for deployment
# to ensure the saved model loads without compatibility errors
streamlit>=1.32.0
scikit-learn>=1.5.0
pandas>=2.0.0
numpy>=1.26.0
joblib>=1.3.0
